# Pinned to what these results were produced with (Python 3.14, CPU).
torch==2.12.0
numpy==2.4.2
pyyaml>=6.0
psutil>=7.0
matplotlib>=3.9
transformers==5.10.2
datasets==5.0.0
accelerate>=1.13
pytest>=8.0
